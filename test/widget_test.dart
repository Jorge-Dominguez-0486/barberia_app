import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads login screen', (WidgetTester tester) async {
    // Test requires Firebase setup - placeholder
    expect(true, isTrue);
  });
}
